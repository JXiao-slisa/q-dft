# slisaDFT Core Principles — 核心规则

## 规则零：绝对不接受编造数据 / Zero Tolerance for Fabricated Data

**这是最高优先级的规则，优先级高于任何其他规则。**

> "计算可以失败，结果可以不如预期，但数据必须是真实的、可追溯的。"

### 具体规定

1. **禁止编造任何数值**。所有能量、力、结构参数、谱图数据必须来自真实的计算或真实的实验测量。

2. **禁止编造任何图表**。所有生成的图表（DOS图、台阶图、结构图等）必须基于真实计算数据绘制。

3. **数据来源必须可追溯**。每个输出结果必须附带其来源引用（计算日志、文件路径、计算时间等）。

4. **失败的诚实优于成功的虚假**。如果某个计算无法完成（VASP未收敛、MLIP模型未下载等），必须如实报告失败原因，而非使用替代/编造数据蒙混过关。

5. **代码可产生编造数据的情况**：工具函数（如 `dos_analysis.py` 中的 `plot_dos`）本身是纯数据可视化工具，不产生数据。但任何调用这些工具的流程必须保证传入真实数据。测试脚本中不得使用合成数据替代真实计算结果。

6. **特殊情况**：在开发阶段，允许使用**已知解析解**或**已发表文献数据**进行代码正确性验证，但必须在输出中明确标注数据来源。

### 执行

- 所有 `test_*.py` 和测试流程必须使用真实计算数据
- 报告生成时，`computational_details` 部分必须包含完整的数据来源说明
- 任何违反此规则的行为视为严重错误

---

## 规则一：可复现性 / Reproducibility

所有计算必须记录完整参数，确保他人可复现结果。

## 规则二：模块化 / Modularity

核心计算逻辑与前端/适配层分离，确保六种产品形态（DSH Plugin / Docker / SaaS / REST API / Jupyter / CaaS）可复用同一核心代码。

## 规则三：开放性 / Openness

开源核心采用 Apache 2.0 许可证。商业插件通过独立公司持有，与开源项目严格隔离。